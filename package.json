{
  "name": "is-surrealdb-production-ready-yet",
  "version": "0.1.0",
  "private": false,
  "description": "Reproducible tests + benchmarks for SurrealDB vector-index behaviour (DiskANN / HNSW). Run with Bun.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "bun tests/diskann.ts all",
    "diskann": "bun tests/diskann.ts all",
    "diskann:reliability": "bun tests/diskann.ts reliability",
    "hnsw": "bun tests/hnsw.ts"
  },
  "engines": { "bun": ">=1.1" }
}
